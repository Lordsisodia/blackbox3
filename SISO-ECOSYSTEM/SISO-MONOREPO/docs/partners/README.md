# Partnerships App Notes

Document all partnership-portal specific refactors, schema updates, and outstanding tasks here.

- Feature specs and story bundles live in `docs/features/partners/portal/`.
- Architecture blueprints moved to `docs/architecture/partners/`.
- Authentication, deployment, and Supabase guides reside in `docs/integrations/**` and `docs/operations/`.
- Use this page for running notes so the migration state stays visible.
