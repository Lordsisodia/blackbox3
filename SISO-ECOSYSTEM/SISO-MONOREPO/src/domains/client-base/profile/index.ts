/**
 * Profile Domain - User Profile & Settings Management
 *
 * This domain provides profile management accessible throughout the dashboard journey.
 * Allows clients to view and edit their information, preferences, and previous step data.
 *
 * Key Features:
 * - View/edit personal information
 * - Manage account settings
 * - Review and update completed steps
 * - Access historical data from all dashboard steps
 * - Update preferences at any time
 *
 * @domain client-base/profile
 * @accessible Throughout entire dashboard (not part of progressive unlock)
 * @allows Editing of previously completed steps
 */

// Pages
// export { default as ProfilePage } from './pages/ProfilePage'

// Components
// export { ProfileHeader, ProfileForm, SettingsPanel, StepHistory } from './components'

// Hooks
// export { useProfile, useProfileUpdate, useStepHistory } from './hooks'

// Types
// export type { ProfileData, UserSettings, StepHistoryItem } from './types'
