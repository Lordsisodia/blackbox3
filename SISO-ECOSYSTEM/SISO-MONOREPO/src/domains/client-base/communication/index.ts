/**
 * Communication Domain - AI Chat & Support System
 *
 * This domain provides context-aware AI chatbot and support functionality accessible
 * throughout the entire dashboard. The AI understands which step the user is on and
 * provides relevant, contextual assistance.
 *
 * Key Features:
 * - Floating chat widget (persistent across all pages)
 * - Full-page chat interface
 * - Context-aware AI responses (knows current dashboard step)
 * - Step-specific help and guidance
 * - Support ticket system (via chat)
 * - CEO escalation workflow
 * - Real-time messaging (WebSockets)
 * - Chat history persistence
 *
 * Context Awareness:
 * - Step 1 (Onboarding): Help with business info questions
 * - Step 2 (Moodboard): Design inspiration suggestions
 * - Step 3 (App Plan): Plan clarifications and questions
 * - Step 4 (Timeline): Progress updates and milestone info
 * - Step 5 (Agent Teams): Agent activity explanations
 * - Step 6 (Payments): Invoice and payment questions
 * - Step 7 (Development): Code progress and technical questions
 * - Step 8 (Testing): QA status and bug reports
 * - Step 9 (Launch): Go-live and deployment questions
 *
 * @domain client-base/communication
 * @accessible Throughout entire dashboard (persistent)
 * @integrates All dashboard steps, profile, documents, tasks
 */

// Pages
// export { default as CommunicationPage } from './pages/CommunicationPage'

// Components
// export { ChatWidget, ChatWindow, MessageList, MessageInput, ContextualHelp, CEOEscalation } from './components'

// Hooks
// export { useChat, useAIContext, useSupportTickets, useRealtime } from './hooks'

// Server
// export { aiChatbot, ticketSystem, realtime } from './server'

// Types
// export type { Message, ChatContext, SupportTicket, AIResponse } from './types'
