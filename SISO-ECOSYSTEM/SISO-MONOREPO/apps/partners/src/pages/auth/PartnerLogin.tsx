/**
 * Partner Login Page
 * Uses new auth feature architecture (@features/auth)
 */

import { LoginForm } from '@features/auth/ui/LoginForm'

export default function PartnerLogin() {
  return <LoginForm />
}
