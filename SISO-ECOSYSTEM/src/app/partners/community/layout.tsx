import type { ReactNode } from "react";

export default function PartnersCommunityLayout({ children }: { children: ReactNode }) {
  return <div className="min-h-screen bg-siso-bg-primary">{children}</div>;
}
